<Page x:Class="uchebka32.Pages.ListCharity"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:uchebka32.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="RegSpons">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.05*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.05*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="0.3*"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalAlignment="Top" VerticalScrollBarVisibility="Hidden">
            <StackPanel>
                <StackPanel Margin="220, 20, 220, 50">
                <TextBlock Text="Список благотворительных организаций" 
                           FontWeight="Medium" 
                           Style="{StaticResource TextHeader}" 
                           Margin="10"/>
                <TextBlock Text="Это - список всех благотворительных учреждений, которые поддерживаются в Marathon Skills 2016. Спасибо за помощь вы поддерживаете их, спонсируя бегунов!"                   
                           FontSize="35" 
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Style="{StaticResource TextPage}" 
                           Margin="70, 0"/>
            </StackPanel>
                
            <ItemsControl x:Name="DataItemsControl" Height="650">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                            <StackPanel Orientation="Horizontal" x:Name="ListPanel">
                            </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            </StackPanel>
            
            
        </ScrollViewer>
        

    </Grid>
</Page>
